#include <stdio.h>

int main(){

    int num[9];

    for (int i = 0; i < 9; i++){
        scanf("%d", &num[i]);
    }
    
    if (num[i] > 1 && num[i]/num[i] = 0){
        printf("%d", num[i]);
    }else{
        printf("%d não é numero primo", num[i]);
    }

    return 0;
}